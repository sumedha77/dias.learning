package com.dias.steps;

import org.testng.annotations.Test;

import com.dias.demoproj.Setup;
import com.dias.utilities.CommonUtility;

public class AllControl extends Setup {
	@Test
	public void chat() throws InterruptedException{
	
	/* This method is to select multiple checkboxes/*
	 * 	
	 */
	//CommonUtility.Selectchkbox(getDriver());
	//SelectDrpdown(getDriver());
	//Dragndrop(getDriver());
	//	Imghover(getDriver());
	}
}
